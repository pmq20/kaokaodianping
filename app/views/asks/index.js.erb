<% if !@asks.blank? %>
  <% @asks.each do |item| %>
    $(".pagination0").before('<%= escape_javascript(render("/asks/ask",:item => item)) %>');
  <% end %>
  current_page = "<%= (params[:page] || "1").to_i + 1 %>";
  <% if @asks.count < @per_page %>
  $(".pagination0").remove();
  <% end %>
<% else %>
  $(".pagination0").remove();
<% end %>

