<div id="shouye_chang">
  <div class="user_profile">
    <%= render "head", :user => @user %>
  </div>
  <ul class="followed-item topic-listing">
    <% @topics.each do |item| %>
      <%= render "topic", :topic => item %>
    <% end %>
  	<% if @topics.length == @per_page %>
  	<li class="pagination last"><a class="page_more" href="#" onclick="return loadAsks();">查看更多...</a></li>
  	<% end %>
  </ul>
<% if @is_ie %><div id="ie_every_foot">&nbsp;</div><div id="ie_every_footer"></div><%end%></div>
<%= render "sidebar", :user => @user %>

